# IPRMS — core dependencies
# Pin/loosen versions as needed once the Databricks runtime is fixed.

# --- Data processing ---
pandas>=2.0
pyspark>=3.5

# --- Schema validation ---
pydantic>=2.5

# --- Config / bundle files (manifest.yaml, policy_pack.yaml) ---
PyYAML>=6.0

# --- Document extraction ---
# Azure AI Document Intelligence = primary OCR/extraction engine
azure-ai-documentintelligence>=1.0.0
azure-core>=1.30
# Fallback parsers for clean digital PDFs
pymupdf>=1.24
pdfplumber>=0.11

# --- Storage / Delta (Databricks) ---
delta-spark>=3.1

# --- API ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# --- Demo UI ---
streamlit>=1.33

# --- Testing ---
pytest>=8.0

# --- Optional: LangGraph internal A→H wrapper (project must work without it) ---
langgraph>=0.2

# --- Optional: LangChain / Azure OpenAI Agent B fallback (project must work without it) ---
langchain>=0.2
langchain-openai>=0.1
openai>=1.30
